/// The publication status for a survey preview.
enum SurveyPreviewStatus {
  /// Survey is saved as draft.
  draft,

  /// Survey is published and can receive answers.
  published,

  /// Survey is closed.
  closed,
}
